################################################################################

# Introduced from C# Version 2.0

# Value types does not support to store null. This problem is solved by this feature (Only for value types).

# Main type os Nullable<T> where T: sturct 

# Shorthand is ? (Nullable operator)

# We can use ?? to store nullable type to non nullable type.

################################################################################